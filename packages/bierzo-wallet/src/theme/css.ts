import { Theme } from "@material-ui/core";

export const itemBackground = "#fcfcfc";

export const getBorderColor = (theme: Theme): string => theme.palette.grey[200];
export const getShadowColor = (): string => "#e3e4e7";
