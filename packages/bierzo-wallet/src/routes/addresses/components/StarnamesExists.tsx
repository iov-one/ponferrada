import { Typography } from "medulas-react-components";
import React from "react";

function StarnamesExists(): JSX.Element {
  return <Typography>There are some starnames. Work in progress view</Typography>;
}

export default StarnamesExists;
