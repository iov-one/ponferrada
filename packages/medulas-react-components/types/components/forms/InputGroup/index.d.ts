import * as React from "react";
interface Props {
  readonly children?: React.ReactNode;
  readonly prepend?: React.ReactNode;
}
declare const InputGroup: ({ children, prepend }: Props) => JSX.Element;
export default InputGroup;
